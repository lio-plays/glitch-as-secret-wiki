<% if (!user) { %>
	<p>Welcome! Please <a href="/login">log in</a>.</p>
<% } else { %>
	<p>Hello, <b><%= user.username %></b>. View your <a href="/profile">profile</a> with <b>logout</b>.</p>
<% } %>

<%- content %>
